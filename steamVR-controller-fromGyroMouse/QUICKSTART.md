# GyroMouse Driver - Быстрый старт

## 🚀 За 5 минут до запуска

### 1. Соберите драйвер (1 мин)

```bash
mkdir build
cd build
cmake ..
cmake --build . --config Release
```

### 2. Установите драйвер (1 мин)

**Закройте SteamVR!**

```bash
# От имени администратора:
cd ..
install_gyromouse.bat
```

### 3. Установите Python библиотеки (2 мин)

```bash
pip install opencv-python opencv-contrib-python scipy numpy hidapi pynput
```

### 4. Запустите тест (1 мин)

```bash
# Терминал 1: Симулятор
python simple_gyromouse_simulator.py

# Терминал 2: SteamVR
<Запустите SteamVR>
```

**Готово!** Контроллер должен появиться и двигаться! 🎉

---

## 🎯 С реальной мышью + ArUco

### 1. Создайте ArUco маркер

1. Откройте: https://chev.me/arucogen/
2. Dictionary: 4x4
3. ID: 0
4. Size: 100mm
5. Распечатайте и приклейте на картон

### 2. Узнайте VID:PID мыши

**Windows:**
1. Device Manager
2. Мыши и иные указывающие устройства
3. Ваша мышь → Свойства → Сведения
4. Hardware IDs: `HID\VID_XXXX&PID_YYYY`

**Пример:** VID_2389&PID_00A8

### 3. Обновите скрипт

В `gyromouse_aruco_tracker.py` строка 146:

```python
gyro_reader = GyroMouseReader(vendor_id=0x2389, product_id=0x00a8)
                                       ↑           ↑
                                    ВАШ VID     ВАШ PID
```

### 4. Запустите

```bash
python gyromouse_aruco_tracker.py
```

- Появится окно с видео
- Покажите маркер камере
- Маркер должен быть обведен красным
- Двигайте маркер → контроллер двигается в VR!

---

## 🔧 Команды

### Пересборка + установка

```bash
cd build
cmake --build . --config Release
cd ..
install_gyromouse.bat
```

### Проверка логов

```bash
# SteamVR логи
notepad "C:\Program Files (x86)\Steam\logs\vrserver.txt"

# Ищите: "GyroMouse Driver v1.0 INIT"
```

### Остановка SteamVR

```bash
taskkill /F /IM vrserver.exe
taskkill /F /IM vrmonitor.exe
```

---

## ⚡ Распространённые проблемы

### Контроллер не появляется

```bash
# 1. Проверьте что симулятор запущен
python simple_gyromouse_simulator.py

# 2. Проверьте логи
notepad "C:\Program Files (x86)\Steam\logs\vrserver.txt"

# 3. Перезапустите SteamVR
taskkill /F /IM vrserver.exe
```

### ArUco не детектируется

- ✅ Маркер хорошо освещен?
- ✅ Маркер ID = 0?
- ✅ Маркер ровный (на картоне)?
- ✅ opencv-contrib-python установлен?

### Мышь не читается

```bash
# Проверьте что библиотека установлена
pip install hidapi

# Проверьте VID:PID в Device Manager
```

---

## 📊 Структура проекта

```
gyromouse_driver/
├── build/                  # Сборка
├── dist/gyromouse/         # Собранный драйвер
│   ├── bin/win64/
│   │   ├── driver_gyromouse.dll
│   │   └── openvr_api.dll
│   └── resources/
│       ├── driver.vrdrivermanifest
│       └── input/gyromouse_profile.json
├── src/                    # Исходники C++
│   ├── main.cpp
│   ├── controller_device.cpp
│   ├── mouse_input_client.cpp
│   └── driver.h
├── resources/              # Ресурсы
│   ├── driver.vrdrivermanifest
│   └── input/gyromouse_profile.json
├── simple_gyromouse_simulator.py     # Тестовый симулятор
├── gyromouse_aruco_tracker.py        # Полный tracker
└── install_gyromouse.bat              # Установка
```

---

## 🎮 Использование в VR

1. Запустите tracker скрипт
2. Запустите SteamVR
3. Контроллер появится автоматически
4. Держите маркер перед камерой
5. Ориентация = гироскоп мыши
6. Позиция = маркер на камере

---

## 💡 Советы

- **Калибруйте камеру** для точного tracking
- **Увеличьте маркер** если не детектируется
- **Хорошее освещение** критично для ArUco
- **Протестируйте симулятор** перед реальным железом

---

Готово! Теперь ваша мышь - VR контроллер! 🎉